# MiroEval — Unified Environment Configuration
# Copy this file to .env and fill in your API keys.

# ── Core LLM (required for all modules) ──────────────────────────────────
OPENAI_API_KEY=""
OPENAI_BASE_URL=""                      # Optional: custom OpenAI-compatible endpoint

# ── Search APIs (required for factual eval) ──────────────────────────────
SERPER_API_KEY=""
JINA_API_KEY=""

# ── Optional: override default judge models ──────────────────────────────
# POINT_QUALITY_MODEL="gpt-5.1"
# PROCESS_EVAL_MODEL="gpt-5.2"

# ── Optional: override concurrency settings ──────────────────────────────
# POINT_QUALITY_MAX_WORKERS=20
# PROCESS_EVAL_MAX_WORKERS=10
# FACTUAL_EVAL_MAX_CONCURRENT=10
# FACTUAL_EVAL_MAX_CHUNKS=10

# ── Optional: factual eval summary LLM ───────────────────────────────────
# SUMMARY_LLM_BASE_URL=""
# SUMMARY_LLM_MODEL_NAME="gpt-5-nano"
# SUMMARY_LLM_API_KEY=""
